<%= render 'sidebar', company: @company %>
<div class="col-sm-7">
    <h2 class="text-center">社員を追加</h2>
    <p id="invite-explain">追加したい社員のメールアドレスを入力して「招待メールを送る」をクリックすると、社員登録の招待メールが届きます。</p>
    <%= form_with local: true, url: admin_invite_path, method: :get do |f| %>
        <section class="form-group">
            <%= f.label :メールアドレスから追加 %>
            <%= f.email_field :email, class: "form-control" %>
        </section>
        <section class="form-group home-link">
            <%= f.submit '招待メールを送る', class: "btn btn-info" %>
        </section> 
    <% end %>
</div>
